coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto

ignore:
  - "ros_ws/build"
  - "ros_ws/install"
  - "ros_ws/src/ThirdParty"
  - "**/vendor"

fixes:
  - "ros_ws/src/EasyNavigation/::"
